name: Build on puss and PR

on:
  push:
  pull_request:

jobs:
  nightly:
    uses: ./.github/workflows/build.yml
  test:
    needs: nightly
    uses: ./.github/workflows/test.yaml
    with:
      url: ${{ needs.nightly.outputs.artifact_url }}
