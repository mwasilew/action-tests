name: Schedule build

on:
  workflow_dispatch:
#  schedule:
#  - cron: "*/5 * * * *"

jobs:
  nightly:
    uses: ./.github/workflows/build.yml@HEAD
  test:
    needs: nightly
    uses: ./.github/workflows/test.yaml@HEAD
    with:
      url: ${{ needs.nightly.outputs.artifact_url }}
