name: Schedule build

on:
  workflow_dispatch:
#  schedule:
#  - cron: "*/5 * * * *"

jobs:
  nightly:
    uses: ./.github/workflows/build.yml
  test:
    needs: nightly
    uses: ./.github/workflows/test.yaml
    with: 
      url: ${{ jobs.nightly.outputs.url }}
